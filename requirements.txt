# Core Django
Django>=4.2,<5.0

# Image upload & processing
Pillow>=10.0.0

# Stripe integration for payments
stripe>=9.0.0

# For managing environment variables
python-dotenv>=1.0.0

# Database driver (for MySQL)
mysqlclient>=2.2.0

# For user authentication via Supabase (optional alternative)
requests>=2.31.0

# Django CORS if you plan to access from frontend separately
django-cors-headers>=4.3.1

# Optional: crispy forms for better form styling
django-crispy-forms>=2.1

# For handling file uploads locally (if not using AWS or Cloudinary)
django-cleanup>=8.0.0

# Optional: for production deployment with whitenoise
whitenoise>=6.5.0
